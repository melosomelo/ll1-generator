import { describe, it, expect, beforeEach } from "@jest/globals";
import CFGrammar from "../src/grammar";

describe("CFGrammar", () => {
  describe("constructor", () => {
    it("should throw when grammar has EOI symbol in some rhs", () => {});
  });
});
